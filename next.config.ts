import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    /* config options here */
    // Note: Next.js automatically excludes files listed in .gitignore from builds
    // All development, test, and config files are excluded via .gitignore
};

export default nextConfig;
